# azure-connectivity-doctor
Azure Connectivity Doctor — cross-platform .NET 10 CLI that diagnoses outbound connectivity (DNS, TCP, TLS, certificates, HTTP, Azure SQL, Azure Service Bus) with evidence-based root-cause analysis and Markdown/JSON reports.
